//: Playground - noun: a place where can play.

/* What is a variable?
 * - Memory area, that is, a part of the computer that is associated with a name, which can store values of a certain type.
 */

import UIKit
// The UIKit framework provides the crucial infrastructure needed construct and manage iOS and tvOS apps.

var userName = "Innavoig"
// To declare a variable in Swift language, it is very easy, just put the "word" var + "variable-name" = "value".
var lastName = ""
lastName = "Snitram"
// Here I declared the variable however, instead of adding value to the time, I decided to add it later.

userName = "Merol"
lastName = "Muspi"
// Already here, I decided to override another value over the value that was with you in this variable.

var myAge = 16
// Excluding the type "String", no other type needs quotation marks when assigning a value.
var floatType = 4.0
var doubleType = 6.0
// What is the difference between the Double and Float types? While Double has decimal precision of 15 houses after the comma, while the Float only 6.
var boolType = false

/*
 * OBS:As I said, these variables are given a certain type, where the types are, as I said in the repository's "README.md" file, Swift is based on the Python language so that it does not need to determine a value for that space Stored, different from other languages. But if I want to define a type ?
 */

var country: String = "United States"

/* "Int" is used for integers
* "Float" is used for numbers with decimal points
* "Double" is used for larger numbers, with decimal points
* "String" Sequence of characters
* "Bool" for true or false
*/

var halfNumber = 50
var intNumber = halfNumber + 100
//In this case, I'm concatenating, that is, I'm picking up the variable that is in another part of the code and bringing it to be attribed along with another value to a new variable.

intNumber = intNumber - 2
intNumber = intNumber * 2
intNumber = intNumber / 2

//--------------------------------------------------------------------------------------------------------------------------------

//: Playgroung - substantivo: um lugar onde você pode "brincar" (testar seu códigos).

/* O que é uma variável ?
 *  - Área de memória, ou seja, uma parte do computador que está, associada a um nome, que pode armazenar valores de um determinado tipo.
 */

import UIKit
// O framework UIKit fornece a infraestrutura crucial necessária para construir e gerenciar aplicativos iOS e tvOS.

var nomeUsuario = "Giovanni"
// Para declarar uma variável na linguagem Swift, é muito fácil, basta colocar a "palavra" var + "nome-da-varivel" = "valor".
var ultimoNome = ""
ultimoNome = "Martins"
// Aqui eu declarei a variável porém, ao invés de adicionar valor na hora, decidi adicionar depois.

nomeUsuario = "Lorem"
ultimoNome = "Ipsum"
// Já aqui, eu decidi sobrepor um outro valor por cima do valor que estava contigo nesta variável.

var minhaIdade = 16
// Tirando o tipo "String", nenhum outro tipo precisa de aspas na hora da atribuição de um valor.
var tipoFloat = 2.0
var tipoDouble = 3.0
// Qual a diferença entre os tipos Double e Float ? Enquanto Double tem precisão decimal de 15 casas após a vírgula, enquanto o Float apenas 6.
var tipoBool = true

/*
 * OBS: Como eu bem disse, essas variáveis recebem um determinado tipo, aonde estão os tipos, como eu disse no arquivo "README.md" do repositório, Swift é baseado na linguagem Python que logo, não precisa determinar um valor para aquele espaço armazenado, diferente de outras linguagens. Mas e se eu quiser definir um tipo ?
 */

var pais: String = "Brasil"

/* "Int" é usado para números inteiros
* "Float" é usado para números com pontos decimais
* "Double" é usado para números maiores, com pontos decimais
* "String" Sequência de caracteres
* "Bool" para verdadeiro ou falso
*/

var metadeNumero = 25
var numeroInteiro = metadeNumero + 50
// Nesse caso, estou concatenando, ou seja, estou pegando a variável que está em uma outra parte do código e trazendo ela para ser atribúida juntamente com um outro valor a uma nova varíavel.

numeroInteiro = numeroInteiro - 2
numeroInteiro = numeroInteiro * 2
numeroInteiro = numeroInteiro / 2
